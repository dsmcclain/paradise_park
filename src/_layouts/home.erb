<!doctype html>
<html lang="en">
  <head>
    <%=render Head.new %>
  </head>
  <body class="<%= page.data[:layout] %> <%= page.data[:page_class] %>">
    <%= render Landing.new %>
    <%= render Showroom.new('images/hillside.jpeg', 'A beautiful meadow') %>
    <%= render Board.new('Stories') %>
    <%= render Board.new('Flowers') %>

    <main>
      <%= content %>
    </main>

        <%= render Footer.new %>
  </body>
</html>
