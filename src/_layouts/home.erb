<!doctype html>
<html lang="en">
  <head>
    <%=render Head.new(page.data[:title])%>
  </head>
  <body class="<%= page.data[:layout] %> <%= page.data[:page_class] %>">
    <%= render HomeTop.new %>
    <%= render HomeHero.new('images/meadow.jpg', 'A beautiful meadow') %>
    <%= render Navbar.new %>

    <main>
      <%= content %>
    </main>

        <%= render Footer.new %>
  </body>
</html>
